<?xml version="1.0" encoding="utf-8"?>
<!--
  Конфигурация AccessibilityService для Air Mouse.

  Нам НЕ нужны события доступности (мы не читаем дерево окон) — поэтому
  accessibilityEventTypes/typeFeedbackTypes минимальны. Главное:
    canPerformGestures = true  → разрешает dispatchGesture (клики/скролл);
    canRetrieveWindowContent = false → не запрашиваем лишний доступ.
  Флаг FLAG_REQUEST_ACCESSIBILITY_BUTTON опущен намеренно.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description" />
